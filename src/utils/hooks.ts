import { useState, useEffect, useCallback, useMemo, useReducer, useContext } from "react";

export const hooks = {
    useState,
    useEffect,
    useCallback,
    useMemo,
    useReducer,
    useContext,
}